rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // 로그인한 사용자만 이미지 업로드 가능, 누구나 읽기 가능
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
